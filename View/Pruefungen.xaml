<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BergNoten.View.Pruefungen"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:BergNoten.View"
    Title="Pruefungen">

    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">

            <ListView x:Name="ListView1">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout Padding="10" Orientation="Horizontal">
                                    <StackLayout.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnItemTapped" />
                                    </StackLayout.GestureRecognizers>

                                    <!--  Füge hier weitere UI-Elemente hinzu, die Teil der ViewCell sind  -->
                                    <Label Text="{Binding Text}" VerticalOptions="Center" />

                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Label Text="Aktuelle Prüfung:" />
            <Label Text="{Binding Pruefung}" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>